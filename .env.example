# Base URL of the Learna AI application
BASE_URL=https://app.learna.ai

# Test user credentials (use a dedicated test account, never production users)
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=your-test-password

# Optional: set to 'true' to run in headed mode locally
HEADED=false
